#pragma once

#include <cstdint>

namespace arkanoid {

// A enumeration representing user input. Access it with e.g
// arkanoid::Input::None
enum class Input : uint8_t { None, Left, Right }; // SKELETON

} // namespace arkanoid
